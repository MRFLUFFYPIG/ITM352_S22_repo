require("./projects_data.js");
var num_products = 5;


for(product_index = 1; product_index <= 5; product_index++) {
    if (product_index <=5) {
        //console.log('Don\'t as for anything else!')
        //process.exit();
    }
    console.log( `${product_index}. ${eval('name' + product_index)}`);
    
    
}

console.log('That\'s all we have!')